# rr — Handoff

## Last Updated
2026-03-18 — `rr init` core logic complete. Session workflow protocol added to CLAUDE.md/SPEC.md.

## What's Done
- **Project scaffolding** — `pyproject.toml` (src layout, `rr` entry point), Python 3.12 pinned, `pyyaml` runtime dep, `pytest` dev dep
- **`rr init`** — `init_project(name, parent)` in `src/rr/init.py`: creates project dir, `project.yaml`, six subdirectories, `index.json`, `CLAUDE.md`, `.gitignore`, git repo + initial commit (7/7 tests passing)
- **`src/rr/git.py`** — `git_init`, `git_commit_all` via subprocess, separate from init logic

## What's Next
- Build `rr file`: move file, update `index.json`, regenerate `index.md`, git commit — start with `tests/test_file.py`
- Before `rr file`, need `src/rr/index.py` (index read/write) — build and test this first as a foundation
- Wire up `src/rr/cli.py` at some point (click or typer TBD) — can defer until commands are working

## Decisions Made (Not Yet in Spec)
- Git operations use `subprocess` (not GitPython or similar) — lightweight, no extra dep
- `index.md` not created on `rr init` — it's generated by index operations; will be created/regenerated by `rr file` and `rr reindex`
- Logging approach agreed: `logging.getLogger(__name__)` per module, INFO for user output, DEBUG for internals — not yet added to `init.py`, add when wiring CLI

## Open Issues
- CLI framework (click vs typer) still undecided — needs a decision before wiring `cli.py`
- `rr init` is missing `index.md` creation (SPEC lists it in the directory structure) — treat as part of `rr file`/`rr reindex` work, or add a stub on init?
