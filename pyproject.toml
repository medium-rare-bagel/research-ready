[tool.uv.workspace]
members = ["packages/*"]

[tool.uv.sources]
rr = { workspace = true }
rr-core = { workspace = true }

[tool.pytest.ini_options]
testpaths = ["tests"]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "rr",
    "rr-core",
]
